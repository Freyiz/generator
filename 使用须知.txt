# 根据sql文件创建对应的数据库对象，并在【当前文件夹】内生成对应的模型层、映射层、持久层、业务层、控制层
# 1.将sql文件放入sql文件夹，确保每个sql文件最后一行内容为exit
# 2.配置config.txt文件
# 3.双击运行run.bat程序
# 4.将生成的文件复制到工程目录的src目录下即可使用
# 5.若不需要建立新的数据库对象，移除或清空sql文件即可

